# Modern Business Notice

This template was imported from [StartBootstrap/startbootstrap-modern-business](https://github.com/StartBootstrap/startbootstrap-modern-business) at commit `7d297106cbda2f04db4696752fcdb5b4dc9cd936`.

The original project is licensed under MIT. The original license text is preserved in `LICENSE.original`.

## Import changes

- Removed upstream `.git`, `node_modules`, and lock files from the imported copy.
- Kept the source Pug/Sass files, build scripts, and generated static `dist` pages under `templates/startbootstrap-modern-business/src`.
- Removed the upstream SB Forms API token attribute and external SB Forms script from the contact page.
- Converted the contact form into a static frontend placeholder with no hosted form service dependency.
- Reviewed the source for API routes, middleware files, server-only entry points, environment variables, and common external service clients after the SB Forms removal.

## Reuse notes

This import is intended as a pure frontend multipage business website remix base using HTML, Pug, Sass, Bootstrap, and vanilla JavaScript.

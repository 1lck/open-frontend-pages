# CleanDocs Template Notice

This template was imported from [lmas3009/cleandocs-template](https://github.com/lmas3009/cleandocs-template) at commit `a9a4e98a69f487e0159a22e937a5eb51d633f706`.

The original project is licensed under MIT. The original license text is preserved in `LICENSE.original`.

## Import changes

- Removed upstream `.git` and editor-local files from the imported copy.
- Added a minimal `package.json` for registry validation and remix metadata.
- Replaced GitHub Gist embed scripts with static local code blocks so the documentation page does not require remote script execution for its examples.
- Replaced the ClipboardJS CDN dependency with a small local copy-to-clipboard handler in `public/js/index.js`.
- Reviewed the source for API routes, middleware files, server-only entry points, environment variables, form submissions, and common external service clients. No runtime backend integration was found in the imported source.
- Kept Google Fonts, Tailwind CDN, and GitHub avatar image references as frontend presentation dependencies from the original template.

## Reuse notes

This import is intended as a pure frontend documentation and knowledge-base remix base using HTML, Tailwind CSS, and vanilla JavaScript.
